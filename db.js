const mongoose = require("mongoose");
const Model = mongoose.Model;

const userSchema = new Model({});
const adminSchema = new Model({});
const courseSchema = new Model({});
const purchaseSchema = new Model({});
